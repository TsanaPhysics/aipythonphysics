\subsection{Exercises \quad / \quad \textthai{แบบฝึกหัด}}
\begin{enumerate}
    \item \textbf{Derivation}: Show that $x(t) = A \cos(\omega_0 t + \phi)$ satisfies the ODE $m \ddot{x} + kx = 0$.
    \item \textbf{Coding}: Increase the noise level to 20\% in the Python code. At what point does the "chirp" pattern of the oscillator become unrecognizable?
\end{enumerate}
